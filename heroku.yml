setup:
    addons:
    - plan: heroku-postgresql
build:
    docker:
        web: bestofbooks/Dockerfile
        database: rdb/Dockerfile.

run: 
    main_app:
        command:
            - java -jar /app.jar
        image: web
