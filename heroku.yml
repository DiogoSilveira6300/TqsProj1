setup:
    addons:
    - plan: heroku-postgresql
build:
    docker:
        web: bestofbooks/Dockerfile
        database: rdb/Dockerfile
release:
    image: database
    command: psql -U user < rdb/init.psql
